import { actions as addNotificationActions, types as addNotificationTypes } from './addNotification';

export const types = {
    ...addNotificationTypes
};

export const actions = {
    ...addNotificationActions
};
